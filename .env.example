# Twitch Application Configuration

# Your Twitch application's Client ID
# Get this from: https://dev.twitch.tv/console/apps
TWITCH_CLIENT_ID=your_client_id_here

# Your Twitch application's Client Secret
# Get this from: https://dev.twitch.tv/console/apps
# IMPORTANT: Keep this secret and never commit it to version control
TWITCH_CLIENT_SECRET=your_client_secret_here

# User Access Token - MUST be generated by the broadcaster account
# Required scope: channel:read:redemptions
#
# OPTION 1: Use the OAuth web server (recommended):
#   1. Run: npm run oauth
#   2. Open http://localhost:3000 in your browser
#   3. Click "Connect with Twitch" and authorize
#   4. Copy the access token from the webpage
#
# OPTION 2: Generate using Twitch CLI:
#   twitch token -u -s "channel:read:redemptions"
#
# IMPORTANT: This token must belong to the same user as TWITCH_BROADCASTER_ID
TWITCH_ACCESS_TOKEN=your_access_token_here

# The broadcaster's user ID (the channel to monitor)
# MUST match the user ID of the account that generated the access token
# This will be displayed in the OAuth flow or get it using:
# - Twitch CLI: twitch api get users -q login=YOUR_USERNAME
# - Online tool: https://www.streamweasels.com/tools/convert-twitch-username-to-user-id/
TWITCH_BROADCASTER_ID=your_broadcaster_id_here

# OAuth Server Configuration (optional)
# Redirect URI must match the one configured in your Twitch application
# Default: http://localhost:3000/callback
REDIRECT_URI=http://localhost:3000/callback

# OAuth Server Port (optional)
# Default: 3000
PORT=3000
